{
  "name": "sshorg-site",
  "private": true,
  "description": "Build tooling for the sshorg.com studio scene. The site itself deploys as plain static files.",
  "type": "module",
  "scripts": {
    "build:story": "sh tools/build-story.sh",
    "test": "node --test tests/*.test.cjs tests/*.test.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.10",
    "gsap": "3.15.0",
    "lenis": "1.3.26",
    "three": "0.186.1"
  }
}
